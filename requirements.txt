# =============================================================================
# Streamlit Cloud deployment requirements
# Only packages the dashboard actually needs at runtime.
# Heavy ML/backend deps (torch, transformers, langchain, chromadb, etc.)
# are excluded — they exceed Streamlit Cloud's install memory limit.
# For local full-stack development use requirements-full.txt instead.
# =============================================================================

# Dashboard
streamlit==1.45.1
plotly==6.0.1
altair==5.5.0
pandas==2.2.3
numpy==2.2.3

# HTTP (used by economic_tool fallback inside try/except)
requests==2.32.3
python-dotenv==1.1.0

# Logging
loguru==0.7.3